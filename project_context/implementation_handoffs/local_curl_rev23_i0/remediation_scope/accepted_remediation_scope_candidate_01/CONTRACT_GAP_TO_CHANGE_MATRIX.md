# Contract Gap to Change Matrix

Canonical repository: `rigolugo/pm_research`  
Canonical base inspected: `cc2964840d197a40d1c4ef567b42eda762c0be0a`  
Controlling accepted scope: `REV23_FINDING4_I0A_SCOPE_REVISION_10`  
Preserved checkpoint evidence: `REV23_FINDING4_I0A_R1_CP_0001_FCF406C4` / `fcf406c4f447945d386467256c07455695db23801400f12be49203ffc2fe35da`  
Checkpoint state: `NOT_ACCEPTED`; authorization effect: `NONE`; static result: `BLOCK — REVISION10_STATIC_CONFORMANCE_BLOCKED`.


| ID | Accepted Revision 10 requirement | Observed checkpoint behavior | Exact owner | Required correction | Result/stop implication | Test obligation | False-unblock risk |
|---|---|---|---|---|---|---|---|
| G1 | Three mandatory source paths change coherently. | Only checkpoint `prepared_evidence.py` bytes exist. | `canonical.py`; `finding4_registry.py`; `prepared_evidence.py` | Materialize each file's assigned responsibility in one atomic source candidate. | No partial state is conformant. | Static exact changed-path check and import-ownership inspection; no execution implied. | One-file remediation can still lack codes/parser and appear superficially improved. |
| G2 | Four public codes and total empty-assurance mappings exist. | Codes absent; older path/cross-member classes used. | `canonical.py` inventory; `prepared_evidence.py` triggers | Add codes exactly once; map every applicable public surface; prohibit older mapping for named conditions. | All four return empty assurance and no extra payload. | Positive inventory, direct trigger, negative non-trigger, multifault precedence, propagation. | A trigger may exist as a string but not be public, total, or propagated. |
| G3 | Closed UnitContext validation is second after unit-kind gate. | Public surfaces inspect only `unit_kind`; later predicates can mask invalid fields. | Applicable public wrappers in `prepared_evidence.py` | Validate family and sequence domains before parsing, registry, physical, descriptor, or delegation. | Invalid -> `ERR_UNIT_CONTEXT_INVALID`; unsupported kind still earlier stop. | Null, alias, bool, range, type, and multifault masking cases. | Adding one check to descriptor set leaves structural/wrapper/unit/dispatch bypasses. |
| G4 | Registry solely decomposes frozen normalized paths into typed bindings. | Checkpoint splits strings, strips suffixes, imports frozen tables, reconstructs identity locally. | `finding4_registry.py`; callers in `prepared_evidence.py` | Add sole matcher/bindings and replace every duplicate parse with typed consumption. | Lexical/grammar mappings preserved; no second parser. | Every grammar, placeholder nullability, alias counterexamples, scan source for forbidden parsing. | New matcher may coexist with reachable old parser and yield conflicting identities. |
| G5 | Typed pre-binding and global lexical/grammar/reuse/family/run reductions precede local nullability/set checks. | Per-descriptor early returns and ad hoc later comparisons. | `prepared_evidence.py` | Add typed results/evidence; collect all; deterministic reduction; separate claim family/run stages. | Lexical > grammar > reuse > family > run > nullability. | Cross-descriptor multifaults, duplicates, contradictory inputs, evidence ordering. | Correct single-descriptor cases can hide wrong set-wide class precedence. |
| G6 | Private reducer owns fixed cardinality, target uniqueness, sidecar relation only. | Retains private ordinal result and `expected_role_counts`. | `prepared_evidence.py` | Remove superseded enum/input/branch/translation; use `expected_fixed_role_counts`; keep variable partition count public. | Old private ordinal code unreachable. | Reflection/domain tests, direct helper tests, public ordinal precedence, variable/fixed count distinction. | Dead-looking enum/branch can remain reachable through direct tests or future caller. |
| G7 | Selected wrapper follows exact 19-predicate order and typed projection. | Registry work before descriptor set; old physical/semantic order; direct old `BindingQuery`. | `prepared_evidence.py` | Reorder; delegate set at predicate 7; project typed query; selected physical then paired physical then semantic. | Exact repeated-code stage precedence; wrapper owns implementation stop. | Each adjacent precedence pair, typed impossibility, direct-construction prohibition, sidecar relation cases. | Tests may assert only terminal codes and miss wrong stage/order. |
| G8 | Unit routes selected JSON objects through wrapper and propagates unchanged. | Unit reconciles directly and emits stops itself. | `prepared_evidence.py` | Build wrapper inputs, sidecar-first, supply paired bytes, return non-success unchanged. | Wrapper reference/implementation/physical/semantic outcomes survive through unit and dispatch. | Direct bypass counterexample, sidecar-first case, missing paired proof, unchanged propagation. | Unit can pass direct reconciliation while wrapper contract remains unexercised. |

## Cross-gap invariant

No gap may be marked corrected unless G1 through G8 are all statically satisfied in the same source candidate. A valid halt or stop is conformance when required; success substitution is forbidden.
