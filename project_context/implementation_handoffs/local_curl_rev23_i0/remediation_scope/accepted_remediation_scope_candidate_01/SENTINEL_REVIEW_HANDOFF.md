# Sentinel Review Handoff

## 1. Status

`REVIEW CANDIDATE — PROFESSOR TO SENTINEL`

## 2. Purpose

Request independent review of a narrowly bounded implementation-remediation specification for the eight verified Revision 10 local-curl defects.

## 3. Canonical base

Canonical repository: `rigolugo/pm_research`  
Canonical base inspected: `cc2964840d197a40d1c4ef567b42eda762c0be0a`  
Controlling accepted scope: `REV23_FINDING4_I0A_SCOPE_REVISION_10`  
Preserved checkpoint evidence: `REV23_FINDING4_I0A_R1_CP_0001_FCF406C4` / `fcf406c4f447945d386467256c07455695db23801400f12be49203ffc2fe35da`  
Checkpoint state: `NOT_ACCEPTED`; authorization effect: `NONE`; static result: `BLOCK — REVISION10_STATIC_CONFORMANCE_BLOCKED`.


## 4. Decision requested

Requested Sentinel decision: `APPROVE`, `BLOCK`, `DEFER`, or `NEEDS VERIFICATION`.

Approval would accept only this specification boundary. It would not authorize implementation or select starting bytes.

## 5. Load-bearing review questions

1. Does one atomic three-source stage avoid all temporary dual-ownership and false-unblock states?
2. Are result triggers and precedence identical to accepted Revision 10, especially global lexical-over-grammar, reuse/family/run, and selected predicates 1-19?
3. Does the UnitContext section preserve the accepted type while closing family/sequence validation without inventing coercion semantics?
4. Is the registry the sole path decomposition owner, with every forbidden parsing shortcut closed?
5. Is the private reducer fully narrowed, including reflected/direct-helper reachability?
6. Does unit-level selected JSON iteration cover the exact accepted member set and sidecar-first behavior?
7. Should ascending `object_ordinal` be accepted as the within-class selected-member tie-break, or must another accepted fixture order be stated?
8. Are the two provenance gaps correctly separated, and is a fresh independently captured start sufficient to bypass historical lineage reliance?
9. Is the internal checksum self-reference convention acceptable?

## 6. Strongest reason to approve

The package maps every verified defect to the exact mandatory owner and closes the principal false-unblock route: a partial `prepared_evidence.py` correction cannot be reviewed as conformant without canonical code inventory and registry parser ownership.

## 7. Strongest reason to block

The accepted Revision 10 excerpts specify sidecar-first propagation but the exact deterministic order among multiple same-class selected JSON members should be independently checked against the complete accepted fixture matrix. If ascending ordinal conflicts with that matrix, Sentinel should block or require correction rather than let the implementation choose.

## 8. Strongest alternative design

Multiple source substages could reduce per-stage file count. It is weaker because canonical result types, registry bindings, and consumers are interdependent and no accepted typed stop prevents all use of an intermediate mixed contract. The submitted atomic source stage is therefore safer.

## 9. Acceptance evidence

Sentinel can review package completeness, canonical identities, exact path closure, requirement-to-owner mapping, total result/precedence tables, negative cases, activity separations, checksums, and self-attack. No project execution is necessary or authorized for specification review.

## 10. Activities still unauthorized after acceptance

Even if Sentinel approves this package, all of the following remain unauthorized until separately proposed and Gustavo-authorized: implementation source authoring, starting-SHA selection, test-source authoring, tests/collection, imports, compilation, lint, typing, coverage, CI, local-data reads, network/subprocess use, artifact generation, Git writes, R2, P1/P2/P3, scoring, probe execution, and gate changes.

## 11. Next handoff

`Professor -> Sentinel for full-package review`.

No Claude prompt is included.
