APPROVE — REV10_STARTING_STATE_AMENDMENT_CANDIDATE_04_ACCEPTED

# Sentinel Acceptance Decision — Revision 10 Starting-State Amendment Candidate 04

## Request and classification

- request: independent specification-amendment review;
- classification: `REVIEW` and `DECIDE`;
- canonical repository: `rigolugo/pm_research`;
- canonical review base: `bc957fe05096b790052d0515773b9e0a2dc88a60`;
- submitted candidate: `REV23_FINDING4_I0A_REVISION_10_STARTING_STATE_AMENDMENT_CANDIDATE_04`;
- submitted ZIP SHA-256: `9b6e05ff09e916b02b990556ee1ef6a37e3bc044a83c317ecfcc60fa65a63193`;
- submitted ZIP size: `74507` bytes;
- submitted member count: `19`.

## Why

Candidate 04 resolves the two verified Candidate 03 defects without redesigning the accepted starting-state architecture.

The accepted package provides actual closed Draft 2020-12 halt and success schemas, normative cross-field validation, complete workflow-domain closure, and exact separation of local commit creation, Sentinel local-commit review, separate Gustavo push authorization, one non-force fast-forward push, and Sentinel remote verification.

## Verified findings

1. The detached ZIP identity, all internal checksums, all 19 members, and deterministic archive reproduction were verified.
2. The exact twelve-path starting matrix remains identical to the accepted worktree capture: eleven baseline matches and checkpoint-modified `prepared_evidence.py` at SHA-256 `fcf406c4f447945d386467256c07455695db23801400f12be49203ffc2fe35da`.
3. Both workflow record schemas are closed, typed, non-coercing, and paired with normative stage-row cross-field validation.
4. The workflow closes over `21` states, `20` stages, `70` stop codes, `20` success codes, and `205` ordered predicate applications.
5. Every stage has one from-state, one ordered predicate sequence, one first-applicable stop per failed predicate, one success code, and one to-state.
6. Source and test delivery each separate local commit, Sentinel review, Gustavo push authorization, non-force fast-forward push, and remote installation verification.
7. Remote advancement halts before push. Pull, merge, rebase, reset, restore, force, rollback, and automatic repair are prohibited.
8. The Windows workspace contract uses regular-file identity rather than Git mode and rejects symlinks, junctions, reparse aliases, alternate aliases, and hard-link substitutions.

## Binding interpretation

A later implementation must enforce actual UTC timestamp validity, normative ordering and uniqueness rules, duplicate JSON-member rejection, exact RFC 8785 canonicalization without numeric coercion, and schema plus cross-field validation before any transition.

The accepted amendment preserves the exact three source-authoring paths, four later test-authoring paths, five baseline-support edit prohibitions, failed-gate non-repair, and open multi-round lineage gap.

## Self-attack

The strongest alternative verdict was `BLOCK` because some cross-field relationships are specified in normative Markdown rather than encoded entirely in JSON Schema. That alternative is rejected: the package explicitly identifies the normative cross-field contract, supplies complete machine-readable stage and applicability tables, makes invalid records state-neutral, and leaves no inference-based transition.

## Authorization

This acceptance is specification-only.

It does not authorize:

- canonical installation automatically;
- workspace creation or archive extraction for implementation;
- source/test authoring or materialization;
- local implementation commits, branch/ref writes, pushes, merges, or remote mutation;
- tests, imports, compilation, lint, typing, coverage, CI, or project execution;
- research-data or network activity;
- checkpoint acceptance or promotion;
- P1/P2/P3, scoring, probe execution, or gate changes.

## Canonical installation

This decision may be installed only through one documentation-only package anchored to exact base `bc957fe05096b790052d0515773b9e0a2dc88a60`. The installation must change no live source, test, dependency, data, runtime, or empirical-artifact path. The resulting commit must be returned to Sentinel for exact installation verification.

Until that verification, the installation state is `PENDING_SENTINEL` and no implementation authorization may be considered active.
