# Claude prompt status — REV23 I0 Amendment 03

**NO ACTIVE IMPLEMENTATION AUTHORIZATION**

Do not use the superseded “Implement REV23 I0 Contract Core” prompt.

Revision 23 with Amendments 01, 02, and 03 is accepted as SPEC ONLY. The earlier implementation authorization applied only to the Amendments-01/02 bytes and does not carry forward.

Claude must not edit source or tests, execute Python or tests, read local research data, invoke curl/subprocess/network paths, produce replay/empirical artifacts, or modify canonical project files.

A future implementation prompt may be created only after:

1. the canonical Amendment 03 update is committed and verified;
2. Gustavo explicitly authorizes a bounded implementation stage;
3. Sentinel issues a new handoff pinned to the new commit and exact accepted hashes.

Until then, stop and return `NO_ACTIVE_IMPLEMENTATION_AUTHORIZATION`.
