# README FIRST — REV23 Amendment 03 Lifecycle-Corrected Materialized Package

This package supersedes every earlier REV23 Amendment 03 package. It corrects only the remaining completed-capture lifecycle contradiction: all three curl completion categories now explicitly permit all three mechanically derived write-out evidence states, every one of the nine pairs persists HTTP reconciliation first, and invalid evidence then produces `STOP_HTTP_WRITEOUT_INVALID` before compatibility or strict body analysis.

All other materialized Amendment 03 decisions remain unchanged, including token-manifest relocation/provenance, 600/496/496 populations, typed-cell and NULL rules, partition identity, pre-run/run identities, the sole HTTP schema version, canonical raw Base64 evidence, retained header-parser binding, the twelve reconciliation combinations, authorization boundaries, denominators, thresholds, result ordering, and downstream gates.

Baseline commit: `226085ca9ba7fa41a8b666005499827d6fa6b9c5`.

Key contents: `accepted_contract_materialized/`, the complete amendment, RFC 6902 patch, byte-reconstructing diffs/replacements, transformation manifest, integrity inventories, static consistency report, and checksums.

Sentinel review is required. This package does not authorize implementation, test authoring/execution, project-code execution, local-data reads, network/curl action, replay, empirical work, downstream phases, or gate changes.
