--- a/ARTIFACT_PRODUCER_CONSUMER_MATRIX_REV23.md
+++ b/ARTIFACT_PRODUCER_CONSUMER_MATRIX_REV23.md
@@ -13,6 +13,7 @@
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/policy/policy_resolution.parquet` | V4 policy resolver | V5 fingerprint readiness, V5A attachment | `table:policy_resolution` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | pre-run |
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/reconciliation/source_b_source_d_reconciliation.parquet` | V3 reconciliation | V5 request builder, V5A attachment, Sentinel | `table:source_b_source_d_reconciliation` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | all rows reconciled |
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/reconciliation/source_b_source_d_reconciliation_summary.json` | V3 reconciliation | V5 request builder, V5A attachment, Sentinel | `json:source_b_reconciliation_summary` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | pre-run |
+| `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/request_manifest/token_manifest.parquet` | V5 token-manifest builder | V5 request-manifest builder, V5A detached attachment builder, V6 run-identity validator, authorization validator, Sentinel | `table:token_manifest` | complete file SHA-256 plus registered row/logical hashes | V5 pre-run; after V3 reconciliation and before request manifest |
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/request_manifest/request_manifest.parquet` | V5 request manifest builder | V5 plan builder, V6 run identity, authorization validator | `table:request_manifest` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | pre-run |
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/request_plan/request_plan.parquet` | V5 request plan builder | V6 run identity, authorization validator, capture | `table:request_plan` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | pre-run |
 | `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/request_plan/request_plan_hashes.json` | V5 request plan builder | V6 run identity, authorization validator | `json:request_plan_hashes` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | pre-run |
@@ -72,7 +73,7 @@
 | `artifacts/local_curl_per_side/runs/<run_id>/policy/pre_v7_metadata.json` | V6/V7 validator | capture, analysis, final inventories | `json:pre_v7_metadata` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | must bind package/contract hashes |
 | `artifacts/local_curl_per_side/runs/<run_id>/policy/run_policy_fingerprint.json` | V6 run establishment | V7 validator, capture, analysis, final inventories | `json:run_policy_fingerprint` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | run-scoped validated fingerprint |
 | `artifacts/local_curl_per_side/runs/<run_id>/reports/result_summary.json` | V9 result engine | V10 replay finalizer, Sentinel | `json:result_summary_v23` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | only structurally valid replay result |
-| `artifacts/local_curl_per_side/runs/<run_id>/request_manifest/token_manifest.parquet` | V5 request-manifest builder | V5 plan builder, V6 run identity, Sentinel | `table:token_manifest` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | complete 600-token manifest |
+
 | `artifacts/local_curl_per_side/runs/<run_id>/resume/resume_journal.parquet` | V7-V9 resume controller | authorization validator, disposition builder, Sentinel | `table:resume_journal` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | records every resume/skip/retry/cancellation decision |
 | `artifacts/local_curl_per_side/runs/<run_id>/stop/stop_state.json` | V7-V10 stop handler | V10 stop finalizer, Sentinel | `json:stop_state` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | no replay label |
 | `artifacts/local_curl_per_side/runs/<run_id>/strict/audit_snapshot_manifest.json` | strict snapshot finalizer | resume, final inventories, Sentinel | `json:audit_snapshot_manifest` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | hash chain uses previous manifest file hash; never self-hashes |
@@ -91,4 +92,4 @@
 | `policies/TRANSPORT_POLICY_REV22.json` | Professor byte-copy packager | Sentinel, future validator | `json:transport_policy` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | package retained governing bytes |
 | `policies/URL_SERIALIZATION_POLICY_REV22.json` | Professor byte-copy packager | Sentinel, future validator | `json:url_serialization_policy` | complete file SHA-256; schema-specific row/logical/semantic hashes also required | package retained governing bytes |
 
-Every row above has one producer and at least one consumer. Duplicate path templates are forbidden.
+Every row above has one producer and at least one consumer. Duplicate path templates are forbidden. The sole token manifest is pre-run; the run-scoped token-manifest path is prohibited.
