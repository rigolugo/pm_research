--- a/CHANGE_LEDGER_REV22_to_REV23.md
+++ b/CHANGE_LEDGER_REV22_to_REV23.md
@@ -10,3 +10,23 @@
 | Reservation-before-start recovery was open | Added terminal `REQUEST_RESERVATION_CANCELLED_NO_START` requiring positive zero-start snapshots and irreversible old-authorization closure. |
 | Result table contained impossible all-clear exact-reproduction branch and causal wording | Replaced with reachable mutually exclusive exact-negative, below-threshold-difference, gain-only-clear, nonmonotone-clear, threshold-inconclusive, stop, and residue outcomes. |
 | Consistency | Updated governing documents, registries, matrices, stops, results, tests, lifecycle, and package hashes; retained internal contract bytes unchanged. |
+
+## REV23 Amendment 03 materialization
+
+| Finding | Amendment 03 correction |
+|---|---|
+| Token manifest was run-scoped before `run_id` existed | Relocated the sole artifact to `artifacts/local_curl_per_side/pre_run_attempts/<pre_run_attempt_id>/request_manifest/token_manifest.parquet` at V5; prohibited the old path and dual copies. |
+| Token/request/plan schemas conflicted | Closed exact 600/496/496 schemas, provenance, typed equality, field order, logical ordering, and hash projections. |
+| Pre-run time was illustrated as a string in one identity formula | Made `created_at_ns` a required JSON integer in the exact five-field semantic object. |
+| Partition identity had a conflicting shorthand | Established the sole seven-field typed projection and actual/snapshot reconciliation rule. |
+| HTTP status mapping was partial | Added a total mutually exclusive mapping and issue codes for generic unequal pairs and `000` plus invalid header evidence. |
+| Package bytes were previously unavailable to Professor's artifact workspace | Verified the uploaded canonical `accepted_contract/` bytes against the accepted inventory, materialized targets, and recomputed all hashes. |
+| Storage widths were presented as logical tags and null retained the non-null tag | Restored the global contract: unsigned widths hash as `UINT`, `INT64` as `INT`, UTC nanoseconds as `UTC_NS`, and every null as `t:"NULL",v:null`. |
+| HTTP schema used `INVALID__ANY_HEADER` | Replaced it with four explicit invalid-writeout branches and eight other exact outer combinations; each branch machine-requires integer nullability/range, state, issue, stop/continuation, and C14–C23 reachability. |
+| Partition identity referenced the broad audit-family enum | Added and bound exact `partition_audit_family={capture,analysis_compatibility,analysis_strict}` while preserving the broader enum for non-partition objects. |
+| No-final-LF one-line transformations were claimed as ordinary diffs | Designated exact complete replacements for the schema registry, governing manifest, and sidecar; retained and verified RFC 6902 reconstruction for the schema registry. |
+| HTTP reconciliation schema version appeared as both `_row.v23` and non-row `.v23` | Selected sole `local_curl_http_status_reconciliation.v23` and synchronized table, field domain, projection, specification, traceability, and assertions. |
+| Invalid curl write-out evidence was not fully representable | Added canonical raw Base64 evidence, raw-byte SHA-256 binding, nullable exact lexeme, and total mechanical derivation over every byte sequence. |
+| Selected-header state/integer could be treated as caller assertions | Bound both fields to the retained response-header parser output over exact hash-verified registered evidence. |
+| Amendment materialization replaced the accepted Sentinel handoff | Restored the complete prior accepted handoff as an exact prefix and appended an Amendment 03 addendum identifying superseded and unchanged clauses. |
+| Completed-capture lifecycle summary excluded malformed/out-of-range stdout for some curl completion categories and bypassed reconciliation in downstream labels | Made all 3 completion categories accept all 3 derived capture write-out states; all 9 pairs persist HTTP reconciliation, invalid then stops, and no completed mapping routes directly to C02/C03/C07-C23. |
