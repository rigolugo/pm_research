# Gustavo Authorization Record — Revision 10 Remediation Source Authoring

## Authorization

On `2026-07-24`, after Sentinel described the next stage as a bounded atomic implementation-source authorization across exactly three source files with test-source authoring and execution excluded, Gustavo stated:

> i approve the authorization package

This records Gustavo's approval of authorization package `REV23_FINDING4_I0A_REVISION_10_REMEDIATION_SOURCE_AUTHORING_01` subject to all activation conditions and boundaries in this directory.

## Authorized purpose

Prepare the minimum Revision 10-conforming source correction required by the accepted remediation scope across exactly:

- `pm_research/local_curl_per_side/canonical.py`;
- `pm_research/local_curl_per_side/finding4_registry.py`;
- `pm_research/local_curl_per_side/prepared_evidence.py`.

The three paths form one atomic implementation candidate. No partial one-file or two-file completion may be represented as conforming.

## Explicit exclusions

This authorization does not include test-source authoring, test collection or execution, imports, compilation, lint, type checking, coverage, CI, local research-data reads, curl/API/RPC/vendor/Dune/general network activity, project execution, artifact regeneration, dependencies, CLI/config/runtime changes, checkpoint restoration/promotion, Git history writes, R2, P1/P2/P3, scoring, probe execution, or gate changes.

Authorization does not become active merely because this record exists. Canonical installation, Sentinel verification, and a clear local source gate are mandatory.
