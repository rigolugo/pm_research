# Professor → Sentinel Review Handoff — Revised S1 Canonical Acceptance Record Candidate 02

## 1. Status

- handoff ID:
  `HANDOFF_PROFESSOR_S1_PRICE_SOURCE_REVALIDATION_CANONICAL_ACCEPTANCE_RECORD_CANDIDATE_02_REVIEW`
- candidate:
  `S1_PRICE_SOURCE_REVALIDATION_CANONICAL_ACCEPTANCE_RECORD_CANDIDATE_02`
- authoring mode: `DOCUMENTATION_ONLY_CANONICAL_RECORD_DRAFT`
- status: `DRAFT_FOR_SENTINEL_REVIEW`
- authorization effect: `NONE`

`S1_PRICE_SOURCE_REVALIDATION_CANONICAL_ACCEPTANCE_RECORD_CANDIDATE_01` is
historical, blocked, non-controlling, and not proposed for canonical
installation. This handoff proposes Candidate 02 only.

## 2. Purpose

Request independent Sentinel review of a documentation-only candidate that
memorializes:

`ACCEPT FINDING — revised S1 establishes S1_SOURCE_VIABLE for the reviewed EC2 fidelity=1, no-interval request method.`

The candidate preserves the historical method-qualified negative, distinguishes
source viability from price-artifact acceptance, and keeps P1 and the probe
blocked.

## 3. Canonical base

- repository: `rigolugo/pm_research`
- branch: `main`
- expected and observed base:
  `aa378029469c518655ec22756243ee62318011b1`
- canonical worktree modified: no
- GitHub write performed: no

The candidate replacements were derived from the exact base files. Unrelated
Revision 23 and Candidate 04 text was retained.

## 4. In scope

- one proposed immutable acceptance record;
- one machine-readable evidence manifest;
- this Professor-to-Sentinel handoff;
- complete replacements for:
  - `project_context/START_HERE.md`;
  - `project_context/PROJECT_STATE.md`;
  - `project_context/DECISION_LOG.md`;
  - `project_context/ARTIFACT_INDEX.md`;
- package readme and checksum inventory.

## 5. Out of scope

No canonical write, installation authorization, S2 authorization, empirical
rerun, archive opening or reprocessing, project-code execution, test execution,
research-data access, vendor/API/network run, price-artifact build, P1/P2/P3,
scoring, probe execution, or gate change occurred or is authorized.

## 6. Key requirements carried by the candidate

1. Historical `interval=max`, fidelity-omitted
   `S1_SOURCE_NOT_VIABLE` remains immutable and method-qualified.
2. Revised `fidelity=1`, interval-omitted method is recorded as
   `S1_SOURCE_VIABLE` only for the fixed Pass-1 sample and reviewed EC2 route.
3. The denominator remains `300 = 248 valid + 52 excluded`, with subclasses
   `50 + 98 + 100 = 248`.
4. Main-run `496 = 494 HTTP 200 + 2 HTTP 400` and the correct
   `S1_REVALIDATION_INCOMPLETE` halt are preserved.
5. The continuation is limited to one unresolved condition, four fixed-chunk
   requests, and is not represented as a rerun or adaptive retry.
6. Final coverage is UP_DOWN `50/50`, OVER_UNDER `98/98`, NAMED_OTHER
   `100/100`, combined `248/248`; all clear `0.95`.
7. Both sides remain independently token-queried; complementarity is diagnostic
   only and no synthesis or winning-token enumeration is permitted.
8. External evidence hashes are identities only, not claims of canonical
   preservation.
9. No accepted canonical-side price artifact exists; P1 remains blocked and
   `named_binary_probe_blocked = true`.
10. S2 remains a possible later SPEC-ONLY authoring task only after acceptance,
    installation, Sentinel verification, and separate Gustavo authorization.

## 7. Assumptions and open decisions

No empirical measurement was inferred beyond the settled reviewed finding.
The manifest uses exact logical identities where no authoritative filename was
provided and records known filenames only where they were available from the
reviewed continuation context.

The empirical Sentinel finding is settled. Sentinel owns the separate decision
whether Candidate 02 faithfully memorializes that finding and is sufficient for
canonical installation.

Any evidence-identity, denominator, method, base-commit, or authorization
conflict requires `BLOCK` or `NEEDS VERIFICATION`.

## 8. Documentation-only self-attack

### Attack 1 — historical negative accidentally erased

Finding: early wording could have allowed unqualified `S1_SOURCE_VIABLE` to
replace the historical decision.

Disposition: corrected. Every state-bearing file now retains
`S1_SOURCE_NOT_VIABLE` for `interval=max`, fidelity omitted, and qualifies the
revised finding by method, sample, and EC2 route.

### Attack 2 — sample mistaken for full-universe validation

Finding: 100% coverage could be misread as full-P0 coverage.

Disposition: corrected. The record, state replacement, decision entry, index,
and manifest state that the finding applies only to the existing stratified
Pass-1 sample and is not full-universe validation.

### Attack 3 — source viability mistaken for price-artifact acceptance

Finding: `S1_SOURCE_VIABLE` could appear to unblock P1.

Disposition: corrected. Each state-bearing artifact states that no
canonical-side decision-time price artifact was built or accepted, P1 remains
blocked, and `named_binary_probe_blocked = true`.

### Attack 4 — 52 exclusions disappear

Finding: using only `248/248` could hide the original sample reconciliation.

Disposition: corrected. `300 = 248 + 52` and subclass
`248 = 50 + 98 + 100` are explicit invariants. The exclusions cannot enter the
valid-window denominator.

### Attack 5 — continuation mistaken for rerun or adaptive retry

Finding: the four requests could look like a rerun of failed requests.

Disposition: corrected. The continuation is explicitly a separately reviewed,
fixed two-chunk-per-side completion for only the unresolved condition, with
union/deduplication before the unchanged global window. It is labeled not a
rerun, adaptive retry, resampling, or outcome-based selection.

### Attack 6 — external hashes mistaken for canonical preservation

Finding: indexing hashes beside canonical records could imply archive storage.

Disposition: corrected. All empirical evidence entries use
`REVIEWED_EXTERNAL_IDENTITY_NOT_ASSERTED_CANONICALLY_STORED`; the Markdown
record and index state the same distinction.

### Attack 7 — candidate accidentally authorizes S2 or execution

Finding: “next possible step” could function as implicit authorization.

Disposition: corrected. S2 is not active. Only Gustavo may separately authorize
Professor to draft S2 SPEC ONLY after this record is accepted, installed, and
Sentinel-verified. All execution and downstream boundaries remain false.

### Attack 8 — stale replacements overwrite Revision 23/Candidate 04 state

Finding: replacing four central files from stale chat context could delete
unrelated current state.

Disposition: corrected. Replacements were copied from exact canonical
`aa378029469c518655ec22756243ee62318011b1` and patched narrowly. Candidate 04
and Revision 23 sections were not reconstructed from memory.

## 9. Strongest alternative design

The strongest alternative is to preserve the historical negative as the sole
current S1 disposition until a full-universe run or a controlled parameter
ablation isolates causality.

That alternative is not selected in this candidate because the submitted
Sentinel decision expressly establishes method-qualified source viability for
the reviewed sample and EC2 route. The candidate preserves the alternative's
valid concerns by refusing full-universe or causal claims and by keeping
price-artifact acceptance and P1 unblocking separate.

## 10. Acceptance evidence requested

Sentinel should independently verify:

- exact base commit;
- exact seven-path proposal;
- complete replacements rather than fragments;
- no unrelated replacement drift;
- JSON parse and manifest/record agreement;
- 300/248/52 and subclass reconciliation;
- request and response counts;
- all evidence hashes;
- package checksum count and matches;
- historical-result preservation;
- P1/probe block preservation;
- absence of implementation or execution authority.

No empirical rerun is requested or authorized.

## 11. Authorization statement

This package is a review candidate only. It does not authorize installation,
implementation, testing, networking, local-data execution, S2, Pass 2,
price-artifact construction, P1/P2/P3, scoring, probe execution, Git writes, or
gate changes.

## 12. Requested Sentinel decision

`APPROVE`, `BLOCK`, `DEFER`, `ACCEPT FINDING`, or `NEEDS VERIFICATION`.

Professor requests `APPROVE` only after independent verification of every
candidate boundary.
